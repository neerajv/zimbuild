version: 2.1
executors:
  my-custom-executor:
    docker:
      - image: ubuntu:18.04
jobs:
  my-job-name:

    executor: my-custom-executor
    steps:
      - checkout
      - run: |
          # ls ; pwd ; whoami;

workflows:
  my-custom-workflow:
    jobs:
      - my-job-name
